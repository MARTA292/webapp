export class ClassDTO {
  constructor(public id: number, public startTime: string, public endTime: string, public day: string, public name: string,
    public room: string, public teacher: string ) {
  }
}
