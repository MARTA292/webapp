export class ClassDTO {
  constructor(public id: string, public startTime: number, public endTime: number, public day: string, public name: string,
    public room: string, public teacher: string ) {
  }
}
